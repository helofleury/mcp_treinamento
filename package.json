{
  "name": "mcp-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "esbuild": "esbuild",
    "build": "esbuild src/main.js --bundle --outfile=dist/index.js",
    "minify": "esbuild src/main.js --bundle --minify --outfile=dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}